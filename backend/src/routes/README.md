# Routes Folder

This folder will define your API endpoints (URLs) and direct incoming requests to the correct controller functions.

For example, `/api/tickets` route definitions will be stored here in future steps.
