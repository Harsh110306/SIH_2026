# Models Folder

This folder will contain your database models (Mongoose schemas).

Models define what your database objects look like (for example, Ticket schema with title, description, status, etc.).
